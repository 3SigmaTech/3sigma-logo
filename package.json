{
    "name": "3sigma-logo",
    "version": "1.0.1",
    "description": "The logo for 3Sigma Technologies",
    "type": "module",
    "scripts": {
        "dev": "run-when-changed --watch '**/{src,bin}/**' --exec 'npm run build'",
        "build": "node ./bin/build.js"
    },
    "devDependencies": {
        "glob": "^9.3.2",
        "node-sass": "^8.0.0",
        "run-when-changed": "^2.1.0"
    }
}